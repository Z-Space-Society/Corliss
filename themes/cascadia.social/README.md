# cascadia.social

Not themed yet, so this deployment wears the default look. The likely overrides,
when the copy is ready:

- `templates/about.html`, `templates/about_system.html`, `templates/about_team.html`
- `templates/_site_title.html` and `templates/_brand.html`
- `templates/_footer_copy.html`
- `static/css/theme.css`, and `static/favicon.svg` plus `static/favicon.ico`

Start each from the app's own copy in `corliss/templates/` or `corliss/static/`.
See [../README.md](../README.md).
